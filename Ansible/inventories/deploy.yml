[deploy:children]
beacon
shard0
shard1

[beacon]
beacon0 ansible_ssh_host={{beacon.0.IP}} spendingkey={{KEY.beacon.0}} node_port={{beacon.0.NODE_PORT}} rpc_port={{beacon.0.RPC_PORT}}
beacon1 ansible_ssh_host={{beacon.1.IP}} spendingkey={{KEY.beacon.1}} node_port={{beacon.1.NODE_PORT}} rpc_port={{beacon.1.RPC_PORT}}
beacon2 ansible_ssh_host={{beacon.2.IP}} spendingkey={{KEY.beacon.2}} node_port={{beacon.2.NODE_PORT}} rpc_port={{beacon.2.RPC_PORT}}

[shard0]
shard0-0 ansible_ssh_host={{shard.0.0.IP}} spendingkey={{KEY.shard.0.0}} node_port={{shard.0.0.NODE_PORT}} rpc_port={{shard.0.0.RPC_PORT}}
shard0-1 ansible_ssh_host={{shard.0.1.IP}} spendingkey={{KEY.shard.0.1}} node_port={{shard.0.1.NODE_PORT}} rpc_port={{shard.0.1.RPC_PORT}}
shard0-2 ansible_ssh_host={{shard.0.2.IP}} spendingkey={{KEY.shard.0.2}} node_port={{shard.0.2.NODE_PORT}} rpc_port={{shard.0.2.RPC_PORT}}

[shard1]
shard1-0 ansible_ssh_host={{shard.1.0.IP}} spendingkey={{KEY.shard.1.0}} node_port={{shard.1.0.NODE_PORT}} rpc_port={{shard.1.0.RPC_PORT}}
shard1-1 ansible_ssh_host={{shard.1.1.IP}} spendingkey={{KEY.shard.1.1}} node_port={{shard.1.1.NODE_PORT}} rpc_port={{shard.1.1.RPC_PORT}}
shard1-2 ansible_ssh_host={{shard.1.2.IP}} spendingkey={{KEY.shard.1.2}} node_port={{shard.1.2.NODE_PORT}} rpc_port={{shard.1.2.RPC_PORT}}

